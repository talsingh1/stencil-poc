name: Node CI

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: npm install, build, and test
      with:
        node-version: '14'
  - run: npm install
  - run: npm test